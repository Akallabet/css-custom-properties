import { Meta, Story } from '@storybook/addon-docs/blocks'
import { Button } from './button'

<Meta
  title="Button"
  component={Button}
  argTypes={{ background: { control: 'color' }, children: { control: 'text' } }}
/>

export const Template = (args) => <Button {...args} />

<Story name="Basic" args={{ children: 'Button' }}>
  {Template.bind({})}
</Story>
